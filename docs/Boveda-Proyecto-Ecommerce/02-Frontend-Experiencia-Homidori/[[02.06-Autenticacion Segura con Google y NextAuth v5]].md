

# [[02.06-Autenticacion Segura con Google y NextAuth v5]]

Documentación de integración de Google OAuth en `ecommerce-basictech`, adaptada a **Next.js 16**, **NextAuth.js v5 (Beta)**, la base de datos PostgreSQL mediante **Prisma 7** y el dominio oficial peruano.

---

## 1. Configuración del Proveedor en Google Cloud Console

Configura las credenciales en tu panel de Google Auth Platform usando el dominio correcto:

1. **Pantalla de Consentimiento (OAuth Consent Screen):**
* Asegúrate de que esté en modo **Externo** para permitir accesos de clientes finales.


2. **Crear Credenciales (ID de cliente de OAuth):**
* **Tipo de aplicación:** Aplicación web.
* **Orígenes de JavaScript autorizados:**
* `http://localhost:3000` *(Entorno local de desarrollo)*
* `[https://latienditadeblue.org.pe](https://latienditadeblue.org.pe)` *(Dominio de producción oficial)* 👈


* **URIs de redireccionamiento autorizadas:**
* `http://localhost:3000/api/auth/callback/google`
* `[https://latienditadeblue.org.pe/api/auth/callback/google](https://latienditadeblue.org.pe/api/auth/callback/google)` 👈





---

## 2. Configuración del Entorno Seguro (`.env` / `.env.local`)

Asegúrate de actualizar la URL base en tus variables de producción para evitar el clásico error `redirect_uri_mismatch`:

```bash
# Google OAuth Credentials
GOOGLE_CLIENT_ID=tu_cliente_id_generado.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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ido

# NextAuth Global Config
NEXTAUTH_SECRET=usa_un_hash_seguro_de_32_caracteres_minimo
NEXTAUTH_URL=https://latienditadeblue.org.pe  # 👈 Cambiado a tu dominio .org.pe en producción

```

---

## 3. Implementación del Backend (NextAuth v5 Beta)

El código en `src/auth.ts` queda exactamente igual de optimizado, mapeando el correo y gestionando los avatares:

```typescript
import NextAuth from "next-auth";
import Google from "next-auth/providers/google";
import { PrismaAdapter } from "@auth/prisma-adapter";
import { prisma } from "@/lib/prisma";

export const { handlers, auth, signIn, signOut } = NextAuth({
  adapter: PrismaAdapter(prisma),
  providers: [
    Google({
      clientId: process.env.GOOGLE_CLIENT_ID,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET,
      profile(profile) {
        return {
          id: profile.sub,
          name: profile.name,
          email: profile.email,
          image: profile.picture,
          username: profile.email ? profile.email.split("@")[0] : `user_${profile.sub.slice(0, 5)}`,
          avatarFileName: null, // Fallback automático a "Mascota BLUE.png"
        };
      },
    }),
  ],
  callbacks: {
    async session({ session, user }) {
      if (session.user) {
        session.user.id = user.id;
        (session.user as any).username = (user as any).username;
      }
      return session;
    },
  },
  pages: {
    signIn: "/login",
  },
});

```

El endpoint `src/app/api/auth/[...nextauth]/route.ts` se limita a exponer el handler:

```typescript
import { handlers } from "@/auth";
export const { GET, POST } = handlers;

```

---

## 🔗 Conexión con Otras Notas de la Bóveda

* **[[03.01-Pasarela Automatizada Mercado Pago]]**: Recuerda actualizar la variable `NEXT_PUBLIC_BASE_URL` a `[https://latienditadeblue.org.pe](https://latienditadeblue.org.pe)` para que las IPs de los Webhooks de Mercado Pago apunten al dominio correcto.

---

¡Listo! Con esto nos aseguramos de que Google no rebote los inicios de sesión por discrepancias en las URLs autorizadas.

¿Cuál es el siguiente paso en nuestra hoja de ruta? ¿Le metemos mano al diseño del checkout o a la generación de correos post-pago?

